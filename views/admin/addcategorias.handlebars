{{#each erros}}
    <div class="alert alert-danger">
        {{texto}}
    </div>

{{else}}

{{/each}}

<h2 class="mb-3">Nova categoria: </h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/categorias/nova" method="POST">
            <label for="nome">Nome: </label>
            <input class="form-control mb-3" type="text" id="nome" name="nome" placeholder="Nome da categoria">
            <label for="slug">Slug: </label>
            <input class="form-control mb-3" type="text" id="slug" name="slug" placeholder="Slug da categoria">
            <button type="submit" class="btn btn-success">Criar categoria</button>
        </form>
    </div>
</div>
